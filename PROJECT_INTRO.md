# 项目介绍：大语言模型在作业车间排程领域的探索

> **DFJSP-NL-LoRA** — 在消费级 GPU 上探索「自然语言进、自然语言出」的动态柔性作业车间调度  
> 作者：[yangdongsheng02](https://github.com/yangdongsheng02) · 仓库：[dfjsp-nl-lora](https://github.com/yangdongsheng02/dfjsp-nl-lora)

---

## 一、为什么关注「大模型 + 排程」

智能制造的核心难题之一，是 **如何在资源约束下快速做出可执行的生产计划**。传统 APS/MES 依赖规则、启发式或数学规划，界面复杂、门槛高。大语言模型（LLM）普及后，一个自然的问题是：

> **计划员能否用自然语言描述现场扰动，由模型直接给出可落地的排程方案？**

这关系到三类价值：

1. **交互方式**：从填表、点选甘特图，转向对话式「急单插队、三号机故障、优先交期」。
2. **知识融合**：模型可能综合工艺说明、历史案例与当前状态，而不只是跑固定规则。
3. **边缘部署**：若小模型 + 低显存微调可行，车间侧 PC 即可试验，无需总是上云大模型。

本仓库即在这一背景下的一次 **边界探测实验**：不追求商业产品，而是回答——在 **4 GB 显卡 + 1.5B 指令模型** 上，**纯自然语言端到端全排程** 是否现实。

---

## 二、排程行业背景：从精确优化到学习式方法

### 2.1 问题本质

**作业车间调度（JSP）** 与 **柔性作业车间（FJSP）** 属于经典 **NP-hard** 组合优化：在机器互斥、工序先后顺序下，为每道工序分配机器与开工时刻，优化 makespan、拖期、换型成本等。

| 技术路线 | 典型代表 | 工业应用 |
|----------|----------|----------|
| 精确优化 | MIP、约束规划 CP-SAT（OR-Tools、Gurobi） | 中小规模高质量解；规模大则限时可行解 |
| 启发式 / 元启发式 | 遗传算法、禁忌搜索、模拟退火 | APS 内核、规则 + 局部搜索 |
| .dispatch 规则 | SPT、EDD、定制优先级 | 实时性强，最优性弱 |
| 强化学习 / GNN | 策略网络学 dispatch | 研究活跃，产线落地仍少 |
| **大语言模型** | SFT、Tool-use、Agent | **2023 起快速兴起，范式未定型** |

动态场景（急单、设备故障、插单）要求 **重调度（rescheduling）**：在已执行部分计划的前提下，对剩余及新增工序重新优化——比静态 FJSP 更贴近真实车间。

### 2.2 大模型进入排程领域的几条路径

学术界与工业界探索并不统一，可粗分为：

```
┌─────────────────────────────────────────────────────────────┐
│                    LLM × 生产排程（示意）                      │
├──────────────┬──────────────┬──────────────┬────────────────┤
│ A. 语义接口  │ B. 启发式    │ C. 端到端    │ D. 解释与协同   │
│ NL → 结构化  │ LLM 生成初始 │ NL → NL/JSON │ 对比方案、问答  │
│ → 求解器算   │ 解或邻域操作 │ 直接出排程   │ 异常诊断        │
└──────────────┴──────────────┴──────────────┴────────────────┘
         ↑ 主流落地预期              ↑ 本仓库探索方向（最「纯」）
```

| 路径 | 做法 | 优点 | 难点 |
|------|------|------|------|
| **A. LLM + 求解器** | 模型解析意图、填参数，CP-SAT/MIP 出解 | 最优性有保障 | 接口设计、幻觉导致约束错误 |
| **B. LLM 作启发式** | 生成初始序列或邻域 move | 结合传统优化 | 需闭环验证与修复 |
| **C. 端到端生成** | 题干 NL → 完整排程 NL/结构文本 | 交互最自然 | **组合爆炸、长输出、难保证可行与最优** |
| **D. Copilot** | 解释、对比、what-if | 易落地 | 不替代核心求解 |

**行业趋势判断（2024–2026）**：短期落地以 **A + D** 为主；**C** 在学术与概念验证中出现，但 **缺乏在真实规模与硬指标上成功的公开案例**，尤其在小模型与边缘硬件上。

---

## 三、本项目的定位与所做之事

### 3.1 探索命题

在 **不调用在线求解器、不要求 JSON 中间格式** 的前提下：

> 微调后的 **Qwen2.5-1.5B-Instruct（4-bit LoRA）** 能否读懂中文车间描述，并 **自由生成** 与 OR-Tools 标签一致的 **完整自然语言排程**（约 30 道工序 × 5 字段 + makespan）？

这是一条刻意的 **高难度基线**：若连此路都不通，可反证「纯 NL 全排程 + 小模型 + 低显存」在当前数据与设定下 **不可行**；若通，则对边缘人机协同有意义。

### 3.2 实验场景（动态 FJSP + 急单）

| 维度 | 规格 |
|------|------|
| 机器 | 4 台（M1–M4） |
| 初始作业 | 6 个，每作业 3–5 道工序，柔性加工（候选机与工时不同） |
| 扰动 | 计划约 50% 进度插入 **1–2 个急单** |
| 题干 | 插队时刻 **t**、机台状态、完成/进行中工序统计 |
| **模型需输出** | **全局完整排程**（非仅未完成片段）：**23–37 道工序/样本**，中位 **30** |
| 标签 | OR-Tools CP-SAT（3s/实例）最优/可行解 → 中文 NL |
| 数据 | 训练 200 / 测试 50；单条序列约 **1200–1450 tokens** |

### 3.3 技术路线概要

1. **数据**：`data_generator.py` 随机实例 + CP-SAT 标签 + 中文题干。  
2. **格式**：`scheduling_format.py`（Qwen chat 模板、NL 解析、strict / jom / makespan 指标）。  
3. **训练**：Unsloth 4-bit LoRA，`train_unsloth.py`（500 step，4 GB VRAM 可训）。  
4. **评估**：`eval_schedule.py`（base → LoRA → 对比；Windows 下 HF 推理）。  
5. **门控**：`overfit_sanity.py`（单样本过拟合预判全量训练是否有效）。

完整配置、日志与数字见 **[实验记录报告](EXPERIMENT_REPORT.md)**。

### 3.4 一句话结论

**本设定下未成功（negative result）**：训练 loss 可收敛至接近 0，但 **自由生成** 的严格工序召回 ≈ 0%，LoRA **未优于** base；模型倾向 **抄写题干中的时刻 t 与可选工时**，而非学会全局最优排程。

---

## 四、问题与讨论

### 4.1 任务设计：叙事与标签的错位

题干强调 **「当前时刻 t 的现场」**；标签却是 **「从 0 起的全局最优完整排程」**（大量工序 `start < t`）。对人而言需「在脑中重放整条时间轴」；对 1.5B 模型而言，**抄 t 是局部最优的生成策略**。这提示：

- **NL 排程任务中，输入叙事坐标系与输出标注坐标系必须一致**；仅靠 system prompt 难以扭转结构性矛盾。

### 4.2 指标陷阱：像对的 ≠ 对的

| 表象 | 实际 |
|------|------|
| 解析成功率 100% | 格式像排程即可通过解析 |
| 训练 loss → 0 | teacher forcing 下能背标签 token |
| 完整方案率 98%（base） | 条数够，**内容几乎全错** |

领域任务必须用 **工序五元组召回、makespan 误差** 等硬指标验收；本仓库的 oracle 50/50 证明 **评测管线可信**，问题在模型而非打分。

### 4.3 容量与数据

约 **30 行 × 5 字段** 的长结构化生成，对 **1.5B + 200 条 SFT** 属于过载；过拟合单条仍 **strict recall 0%**，说明瓶颈不只在数据量，还在 **任务形式与模型先验**。

### 4.4 与「改题」的边界

曾讨论相对时间、只输出未完成工序、JSON 输出、LLM+求解器混合等——均能缓解难度，但均 **改变「纯 NL 全排程」命题**。本仓库 **坚持原命题** 得到负结果；改题应作为 **新实验** 而非同一结论的补丁。

### 4.5 工程侧收获（可复用）

- 4 GB 上 Unsloth LoRA 训通 1536 长度；HF QLoRA 同配置过慢。  
- Windows：评估宜用 HF `generate`，LoRA base 须 `unsloth/` 命名空间。  
- **单样本过拟合门控** 可节省数小时无效全量训练。

---

## 五、未来发展探讨

### 5.1 近期（1–2 年）：混合架构将成主流

**LLM 负责「听懂人」+ 求解器负责「算得对」** 仍是最稳路径：自然语言 → 约束与参数 → CP-SAT/MIP；LLM 并行承担解释、对比方案、异常问答（Copilot）。纯 NL 全排程 **难以单独承担生产核心**。

### 5.2 中期：结构化生成与约束解码

若继续探索端到端，方向可能是：

- **Grammar / JSON schema 约束解码**，保证可行性与字段合法；  
- **分阶段**：先 J-O-M（机器指派），再时间；  
- **更大模型 + 偏好优化（DPO）**，显式惩罚「抄 t」类输出；  
- **检索增强**：相似历史实例 few-shot，而非仅靠 200 条参数记忆。

### 5.3 边缘与小模型

4–8 GB 显卡更适合：**短输出、强约束、Tool-call** 子任务，而非 **30+ 行自由最优排程**。边缘价值在 **现场交互与意图理解**，不在替代 APS 内核。

### 5.4 对本领域的意义

公开 **可复现的负结果** 与完整流水线，有助于：

- 避免重复「loss 很低、解析率很高」的误判；  
- 为论文与产品选型提供 **边界参照**；  
- 将讨论从「能不能跑通」推进到「**何种接口 + 何种规模 + 何种硬件** 才值得投入」。

---

## 六、文档与代码导航

| 文档 | 内容 |
|------|------|
| **[PROJECT_INTRO.md](PROJECT_INTRO.md)** | 本文件：行业背景、LLM 排程探索、问题讨论、展望 |
| **[EXPERIMENT_REPORT.md](EXPERIMENT_REPORT.md)** | 实验记录：配置、结果表、失败模式、原因分析、复现命令 |
| **[README.md](README.md)** | 仓库入口：快速开始与目录索引 |

| 结果文件 | 说明 |
|----------|------|
| [eval_nl_comparison.csv](eval_nl_comparison.csv) | base / LoRA 汇总指标 |
| [eval_nl_samples.csv](eval_nl_samples.csv) | 逐样本预测片段 |

---

## 七、许可与引用

MIT License · 引用示例：

```bibtex
@misc{dfjsp_nl_lora_2026,
  title        = {DFJSP-NL-LoRA: Exploring LLM End-to-End Scheduling on 4GB GPU},
  author       = {yangdongsheng02},
  year         = {2026},
  howpublished = {\url{https://github.com/yangdongsheng02/dfjsp-nl-lora}},
  note         = {Negative result baseline for natural-language job shop scheduling}
}
```

---

*本介绍侧重领域脉络与探索动机；实验细节与原始数据请以 [EXPERIMENT_REPORT.md](EXPERIMENT_REPORT.md) 为准。*
